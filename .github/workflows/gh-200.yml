name: CoHack  Journey

on: push

jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run:  echo "Hello From GH-200 session ! lets create somegreat Workflow files"

      - name: Checkout repo
        uses: action/checkout@v6
        
      - name: List files
        run: ls -la
